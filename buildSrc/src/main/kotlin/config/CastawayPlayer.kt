package config

object CastawayPlayer {

  const val namespace = "io.github.lazyengineer.castawayplayer"

  const val compileSdk = App.compileSdk
  const val minSdk = App.minSdk
  const val targetSdk = App.targetSdk
  const val versionCode = 1
  const val versionName = "1.0.0"

  const val buildTools = App.buildTools
  const val testRunner = App.testRunner

  val jvmTarget = App.jvmTarget
  val javaVersion = App.javaVersion
}
